# --- Talos / Kubernetes Secrets ---
# Base secrets file generated by 'talosctl gen secrets'
secrets.yaml
secrets.y*ml

# Talos client configuration (Admin Access)
talosconfig

# Kubernetes kubeconfig
kubeconfig
.kube/config

# Generated Machine Configs (contain secrets)
controlplane.yaml
controlplane*.yaml
worker.yaml
worker*.yaml

# Cluster backups/snapshots
*.snapshot
*.etcd

# --- WireGuard / Network Security ---
# Private Keys
*.key
*.priv

# Public Keys (Optional: keep if you want to share them publically, exclude if private)
# *.pub

# Configuration files containing keys
wg0.conf
client.conf
mobile-config.conf

# --- ISO / VM Images ---
*.iso
*.img
*.qcow2

# --- OS / IDE / General ---
# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# MacOS
.DS_Store

# Linux
*~
*.swp

# IDEs
.idea/
.vscode/*
!.vscode/settings.json
*.sublime-workspace
